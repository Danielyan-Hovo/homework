import React from 'react';

const Numeric = () => {
    return (
        <input className='f-input' type="number" minLength='1' required name="age" placeholder="age"/>
    );
};

export default Numeric;
